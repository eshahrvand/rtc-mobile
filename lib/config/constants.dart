class AppConstants {
  AppConstants._();

  // Dio Timeouts
  static const Duration connectionTimeout = Duration(seconds: 5);
  static const Duration receiveTimeout = Duration(seconds: 9);
  static const Duration sendTimeout = Duration(seconds: 9);
}
